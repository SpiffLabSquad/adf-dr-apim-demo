<!--
  REFERENCE COPY of the APIM operation policy for:  GET /adf/health

  Returns a static 200 so Azure Traffic Manager's HTTPS health probe can mark the
  endpoint healthy. __REGION__ is substituted at deploy time by
  infra/modules/apim.bicep.
-->
<policies>
  <inbound>
    <base />
    <return-response>
      <set-status code="200" reason="OK" />
      <set-header name="Content-Type" exists-action="override">
        <value>text/plain</value>
      </set-header>
      <set-header name="X-Served-Region" exists-action="override">
        <value>__REGION__</value>
      </set-header>
      <set-body>OK from __REGION__</set-body>
    </return-response>
  </inbound>
  <backend>
    <base />
  </backend>
  <outbound>
    <base />
  </outbound>
  <on-error>
    <base />
  </on-error>
</policies>
